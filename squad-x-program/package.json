{
  "name": "squad-x-program",
  "version": "0.0.1",
  "description": "Squad X Program",
  "scripts": {
    "clear": "rimraf ./program",
    "generate": "codigo/codigo_amd64 solana generate ./cidl.yaml -a",
    "gen:anchor": "bun clear && bun generate",
    "build:anchor": "cd program && anchor build"
  },
  "dependencies": {
    "rimraf": "^5.0.5"
  }
}
